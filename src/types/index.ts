export interface FeatureCardProps {
  title: string;
  description: string;
  image: string;
}
export interface StoryCardProps {
  title: string;
  description: string;
  image: string;
}

export interface NavItem {
  name: string;
  url: string;
}
